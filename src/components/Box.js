import styled from 'styled-components';
import { 
    color, 
    space, 
    layout,
    flexbox,
    background,
    border,
    borderRadius,
    position,
 } from 'styled-system';

export const Box = styled('div')(
    color, 
    space, 
    layout,
    flexbox,
    background,
    border,
    borderRadius,
    position
);